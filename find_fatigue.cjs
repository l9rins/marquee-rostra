const fs = require("fs"); const buf = fs.readFileSync("inspiration_repo/RED MC/NBA Year 2013-14.ROS"); const BLOCKSIZE = 911; const PLAYERS_OFFSET = 369394; const NUM_PLAYERS = 500; let stats = Array(BLOCKSIZE).fill(0).map(() => ({min: 255, max: 0, sum: 0})); for (let p=0; p<NUM_PLAYERS; p++) { const start = PLAYERS_OFFSET + p*BLOCKSIZE; for (let i=0; i<BLOCKSIZE; i++) { const val = buf[start+i]; stats[i].min = Math.min(stats[i].min, val); stats[i].max = Math.max(stats[i].max, val); stats[i].sum += val; } } for (let i=0; i<BLOCKSIZE; i++) { const avg = stats[i].sum / NUM_PLAYERS; if (avg >= 85 && avg <= 101 && stats[i].max <= 100 && stats[i].max >= 95) { console.log(`Byte ${i}: min=${stats[i].min} max=${stats[i].max} avg=${avg.toFixed(1)}`); } }
